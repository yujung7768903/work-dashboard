# 예외 기록

규칙대로 안 굴러간 사례. 다음에 같은 걸 반복하지 않기 위한 기록임

| 세션 아이디 | 기대 동작 | 실제 동작 |
| --- | --- | --- |
| aaf766dd-55ed-49b2-9853-904e1ad8ff17 | 워크트리에서 작업을 끝냈으면 그 브랜치 서버를 띄운 채로 남겨 사용자가 화면을 확인할 수 있게 함 (`run` 스킬 + Stop 훅 `worktree_serve.py` 가 `url:` 줄을 요구) | 검증용으로 9084 를 직접 띄웠다가 확인이 끝나자 `pkill` 로 죽임. 사용자가 지적한 뒤에야 `run` 스킬로 다시 띄움 |
| cfe5d3f4-5c2a-4c20-ad38-ba0f72f4abb7 | 자율 세션은 커밋하지 않는 것이 규칙이므로(`autorun-prompt`), 할일 57 을 done 으로 내리면 목록에서 **사람이 봐야 할 상태**로 보여야 함 | 그냥 done 으로 보여 끝난 일로 읽혔음. 사용자는 화면에 입력칸이 없어 미완성을 발견함. 확인 필요(`review`) 결과가 이 구멍을 메움 |
| 897f6247-20ea-4e7f-8581-4d68447b6d6d | 할일 57 의 "할일 추가 폼" 이 어느 폼인지 확인하고 고침 | 워크트리에 남아 있던 diff(빠른 추가 폼 + `prompt()` 3연속)를 검토 없이 그대로 커밋·병합. 사용자가 말한 곳은 워크스페이스 카드 `+` 버튼 팝업이었고, 연속 `prompt` 는 브라우저가 억제해 제목만 떴음 |
