# 예외 기록

규칙대로 안 굴러간 사례. 다음에 같은 걸 반복하지 않기 위한 기록임

| 세션 아이디 | 기대 동작 | 실제 동작 |
| --- | --- | --- |
| aaf766dd-55ed-49b2-9853-904e1ad8ff17 | 워크트리에서 작업을 끝냈으면 그 브랜치 서버를 띄운 채로 남겨 사용자가 화면을 확인할 수 있게 함 (`run` 스킬 + Stop 훅 `worktree_serve.py` 가 `url:` 줄을 요구) | 검증용으로 9084 를 직접 띄웠다가 확인이 끝나자 `pkill` 로 죽임. 사용자가 지적한 뒤에야 `run` 스킬로 다시 띄움 |
| cfe5d3f4-5c2a-4c20-ad38-ba0f72f4abb7 | 할일 57(빠른 추가 폼에 착수 조건·note 입력)을 커밋·master 병합까지 끝내고 done 으로 내림 | 워크트리에 미커밋 변경만 남긴 채 done 처리. 화면에 입력칸이 없어 사용자가 발견함. 커밋이 없어 `git log` 로도 안 잡혔음 |
